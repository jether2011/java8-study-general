package com.jetherrodrigues.oca.chapter4.city;

import com.jetherrodrigues.oca.chapter4.school.Classroom;

public class School {
	public static void main(String[] args) {
		//Classroom classroom = new Classroom(101, "Mrs. Anderson"); // the code not compile
		//System.out.println(Classroom.globalKey); // the code not compile because the 
		                                         // attibute is static but the modifier is default 
	}
}
